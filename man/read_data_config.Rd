% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/read_data_config.R
\name{read_data_config}
\alias{read_data_config}
\title{Configure Matrix Data for a Tree Analysis}
\usage{
read_data_config(tree_analysis, data)
}
\arguments{
\item{tree_analysis}{[\code{TreeAnalysis}]\cr
A \link{TreeAnalysis} object to add the data to.}

\item{data}{[\code{AbstractCharacterMatrix} or \code{MultiCharacterMatrix}]\cr
One or more \link{DiscreteMatrix} or \link{ContinuousMatrix} objects created by
\code{\link[=create_matrix]{create_matrix()}}.}
}
\value{
The modified \link{TreeAnalysis} object with data commands attached.
Note that unlike \code{\link[=set_tree_search]{set_tree_search()}}, \code{\link[=set_weighting]{set_weighting()}}, and
\code{\link[=set_support]{set_support()}}, this function modifies \code{tree_analysis} in place
rather than returning a clone.
}
\description{
Add one or more character–taxon matrices to an existing \link{TreeAnalysis},
automatically creating the necessary \link{ReadDataCommand} and
\link{CharacterCodingCommand} objects from the matrix settings.

Character inactivity and ordering are read directly from the matrix
objects and translated into the correct character index offsets when
multiple matrices are combined.
}
\examples{
\dontrun{
# --- Discrete matrix ---
nex_path <- system.file("extdata", "canale_2022.nex", package = "nitro")
dm <- ReadAsPhyDat(nex_path) |> create_matrix()
dm$ordered <- c(3, 7)
dm$inactive <- c(1)

ta <- TreeAnalysis$new()
read_data_config(ta, dm)

# --- Combined discrete + continuous ---
nex_path2 <- system.file("extdata", "raven_2017.nex", package = "nitro")
csv_path  <- system.file("extdata", "raven_2017.csv", package = "nitro")
dm2 <- ReadAsPhyDat(nex_path2) |> create_matrix()
cm  <- read.table(csv_path, sep = ",", header = TRUE) |> create_matrix()

ta2 <- TreeAnalysis$new()
read_data_config(ta2, c(dm2, cm))
}

}
\seealso{
\itemize{
\item \code{\link[=create_matrix]{create_matrix()}} — creates the matrix objects required by \code{data}.
\item \code{\link[=make_tree_analysis]{make_tree_analysis()}} — the recommended way to create a
\link{TreeAnalysis} with data already attached.
\item \link{ReadDataCommand} — the command created for the matrix data.
\item \link{CharacterCodingCommand} — the command created for inactive and
ordered characters.
}
}
\keyword{internal}
