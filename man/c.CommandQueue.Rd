% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/CommandQueue-class.R
\name{c.CommandQueue}
\alias{c.CommandQueue}
\title{Combine CommandQueue Objects}
\usage{
\method{c}{CommandQueue}(...)
}
\arguments{
\item{...}{Two or more \link{CommandQueue} objects.}
}
\value{
A single \link{CommandQueue} containing all commands from the input
queues, sorted by priority.
}
\description{
Merges two or more \link{CommandQueue} objects into a single queue by
inserting all commands from each queue into the first, preserving their
original priorities.

This is used by \code{\link[=resolve_dependencies]{resolve_dependencies()}} to concatenate the per-command
cached queues into the final queue returned to \code{\link[=execute_analysis]{execute_analysis()}}.
}
\seealso{
\itemize{
\item \link{CommandQueue} — the queue class.
\item \code{\link[=resolve_dependencies]{resolve_dependencies()}} — the primary caller of this function.
}
}
\keyword{internal}
