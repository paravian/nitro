% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/create_matrix.R
\name{create_matrix}
\alias{create_matrix}
\title{Create a Character Matrix}
\usage{
create_matrix(data, ...)
}
\arguments{
\item{data}{[\code{phyDat} or \code{data.frame}]\cr
The character data. Supply a \link[phangorn:phyDat]{phyDat} object for
discrete characters, or a \code{data.frame} (with a \code{"taxon"} column and
one numeric column per character) for continuous characters.}

\item{...}{Optional named arguments passed to the constructor of the
selected matrix class. See \link{DiscreteMatrix} and \link{ContinuousMatrix}
for available parameters.}
}
\value{
A \link{DiscreteMatrix} or \link{ContinuousMatrix} object, depending on
the class of \code{data}.
}
\description{
Create a discrete or continuous character–taxon matrix object. The type
of matrix created is determined automatically from the class of \code{data}:
a \link[phangorn:phyDat]{phyDat} object produces a \link{DiscreteMatrix}, and a
\code{data.frame} produces a \link{ContinuousMatrix}.
}
\examples{
# --- Discrete matrix ---
nex_path <- system.file("extdata", "canale_2022.nex", package = "nitro")
dm <- ReadAsPhyDat(nex_path) |> create_matrix()
dm

# Mark some characters as ordered
dm$ordered <- c(1, 4, 9)

# --- Continuous matrix ---
csv_path <- system.file("extdata", "raven_2017.csv", package = "nitro")
cm <- read.table(csv_path, sep = ",", header = TRUE) |> create_matrix()
cm

# --- Combined discrete + continuous ---
nex_path2 <- system.file("extdata", "raven_2017.nex", package = "nitro")
dm2 <- ReadAsPhyDat(nex_path2) |> create_matrix()
combined <- c(dm2, cm)

}
\seealso{
\itemize{
\item \code{\link[=make_tree_analysis]{make_tree_analysis()}} — accepts the matrix objects created here.
\item \link{DiscreteMatrix} — for discrete character data; accepts \code{ordered} and
\code{inactive} arguments.
\item \link{ContinuousMatrix} — for continuous character data; accepts an
\code{inactive} argument.
\item \code{\link[=c.AbstractCharacterMatrix]{c.AbstractCharacterMatrix()}} — combine multiple matrices for use in
a \link{TreeAnalysis}.
\item \link{TreeAnalysis} — the analysis container that accepts matrix objects.
}
}
