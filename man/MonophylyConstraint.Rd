% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/MonophylyConstraint-class.R
\name{MonophylyConstraint}
\alias{MonophylyConstraint}
\title{Monophyly Constraint}
\description{
An \link[R6:R6Class]{R6} class that configures a monophyly constraint for
use in constrained tree searches in \pkg{nitro}.

A monophyly constraint requires (positive) or forbids (negative) a
specified set of taxa from forming a monophyletic group in the
recovered trees. An optional set of floating OTUs may also be
specified; these are taxa that are allowed to fall inside the
constrained group but are not required to do so.
}
\details{
\subsection{Fixed and floating OTUs}{
\itemize{
\item \verb{$fixed_otus} — taxa that must (positive) or must not (negative) form
a clade. At least two taxa are required.
\item \verb{$floating_otus} — taxa that are permitted to fall within the
constrained clade but are not required to. Must be disjoint from
\verb{$fixed_otus}. Optional.
}
}

\subsection{Default values}{\tabular{ll}{
   Parameter \tab Default \cr
   \code{floating_otus} \tab \code{NULL} \cr
   \code{is_positive} \tab \code{TRUE} \cr
}

}

\subsection{Combining constraints}{

Multiple \code{MonophylyConstraint} objects can be combined into a
\code{MultiMonophylyConstraint} list using \code{c()}. This is handled
automatically by \link{TopologicalConstraintsCommand} when a list of
constraints is supplied.
}
}
\examples{
# Positive monophyly constraint (taxa must form a clade)
mc <- MonophylyConstraint$new(
  fixed_otus = c("TaxonA", "TaxonB", "TaxonC")
)

# Negative constraint (taxa must not form a clade)
mc_neg <- MonophylyConstraint$new(
  fixed_otus  = c("TaxonA", "TaxonB"),
  is_positive = FALSE
)

# With floating OTUs
mc_float <- MonophylyConstraint$new(
  fixed_otus    = c("TaxonA", "TaxonB", "TaxonC"),
  floating_otus = c("TaxonD", "TaxonE")
)

}
\seealso{
\itemize{
\item \link{BackboneConstraint} — constrains searches using a reference tree.
\item \link{AbstractConstraint} — parent class defining \verb{$is_positive}.
\item \link{TopologicalConstraintsCommand} — holds and renders one or more
constraints as a TNT \code{force} command.
\item \code{\link[=set_constraint]{set_constraint()}} — recommended way to attach constraints to a
\link{TreeAnalysis}.
}
}
\section{Super class}{
\code{\link[nitro:AbstractConstraint]{AbstractConstraint}} -> \code{MonophylyConstraint}
}
\section{Active bindings}{
  \if{html}{\out{<div class="r6-active-bindings">}}
  \describe{
    \item{\code{fixed_otus}}{[\code{character}]\cr
A character vector of taxon names that must (positive constraint)
or must not (negative constraint) form a monophyletic group. Must
contain at least two unique, non-missing names. Must be disjoint
from \verb{$floating_otus}.}

    \item{\code{floating_otus}}{[\code{character} or \code{NULL}]\cr
An optional character vector of taxon names that are permitted to
fall within the constrained clade but are not required to. Must
contain at least one unique, non-missing name if supplied. Must be
disjoint from \verb{$fixed_otus}. Set to \code{NULL} (default) to disable.}
  }
  \if{html}{\out{</div>}}
}
\section{Methods}{
\subsection{Public methods}{
  \itemize{
    \item \href{#method-MonophylyConstraint-format}{\code{MonophylyConstraint$format()}}
    \item \href{#method-MonophylyConstraint-initialize}{\code{MonophylyConstraint$new()}}
    \item \href{#method-MonophylyConstraint-print}{\code{MonophylyConstraint$print()}}
    \item \href{#method-MonophylyConstraint-clone}{\code{MonophylyConstraint$clone()}}
  }
}
\if{html}{\out{<hr>}}
\if{html}{\out{<a id="method-MonophylyConstraint-format"></a>}}
\if{latex}{\out{\hypertarget{method-MonophylyConstraint-format}{}}}
\subsection{\code{MonophylyConstraint$format()}}{
  Format the constraint as a summary table.
  \subsection{Usage}{
    \if{html}{\out{<div class="r">}}
    \preformatted{MonophylyConstraint$format(...)}
    \if{html}{\out{</div>}}
  }
  \subsection{Arguments}{
    \if{html}{\out{<div class="arguments">}}
    \describe{
      \item{\code{...}}{Not used.}
    }
    \if{html}{\out{</div>}}
  }
  \subsection{Returns}{
    A \code{data.frame} with columns for description and value.
  }
}

\if{html}{\out{<hr>}}
\if{html}{\out{<a id="method-MonophylyConstraint-initialize"></a>}}
\if{latex}{\out{\hypertarget{method-MonophylyConstraint-initialize}{}}}
\subsection{\code{MonophylyConstraint$new()}}{
  Create a new \code{MonophylyConstraint} object.
  \subsection{Usage}{
    \if{html}{\out{<div class="r">}}
    \preformatted{MonophylyConstraint$new(fixed_otus, floating_otus = NULL, is_positive = TRUE)}
    \if{html}{\out{</div>}}
  }
  \subsection{Arguments}{
    \if{html}{\out{<div class="arguments">}}
    \describe{
      \item{\code{fixed_otus}}{[\code{character}]\cr
Taxon names forming the constrained group. See the \verb{$fixed_otus}
field.}
      \item{\code{floating_otus}}{[\code{character} or \code{NULL}]\cr
Optional taxon names permitted inside the constrained group
(default: \code{NULL}). See the \verb{$floating_otus} field.}
      \item{\code{is_positive}}{[\code{logical(1)}]\cr
Whether the constraint is positive (default: \code{TRUE}). See the
\verb{$is_positive} field.}
    }
    \if{html}{\out{</div>}}
  }
  \subsection{Returns}{
    A new \code{MonophylyConstraint} object.
  }
}

\if{html}{\out{<hr>}}
\if{html}{\out{<a id="method-MonophylyConstraint-print"></a>}}
\if{latex}{\out{\hypertarget{method-MonophylyConstraint-print}{}}}
\subsection{\code{MonophylyConstraint$print()}}{
  Print a brief summary of the constraint.
  \subsection{Usage}{
    \if{html}{\out{<div class="r">}}
    \preformatted{MonophylyConstraint$print(...)}
    \if{html}{\out{</div>}}
  }
  \subsection{Arguments}{
    \if{html}{\out{<div class="arguments">}}
    \describe{
      \item{\code{...}}{Not used.}
    }
    \if{html}{\out{</div>}}
  }
}

\if{html}{\out{<hr>}}
\if{html}{\out{<a id="method-MonophylyConstraint-clone"></a>}}
\if{latex}{\out{\hypertarget{method-MonophylyConstraint-clone}{}}}
\subsection{\code{MonophylyConstraint$clone()}}{
  The objects of this class are cloneable with this method.
  \subsection{Usage}{
    \if{html}{\out{<div class="r">}}
    \preformatted{MonophylyConstraint$clone(deep = FALSE)}
    \if{html}{\out{</div>}}
  }
  \subsection{Arguments}{
    \if{html}{\out{<div class="arguments">}}
    \describe{
      \item{\code{deep}}{Whether to make a deep clone.}
    }
    \if{html}{\out{</div>}}
  }
}

}
