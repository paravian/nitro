% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/new_tree_search.R
\name{set_tree_search}
\alias{set_tree_search}
\title{Add a Tree Search to a Tree Analysis}
\usage{
set_tree_search(tree_analysis, name, ...)
}
\arguments{
\item{tree_analysis}{[\code{TreeAnalysis}]\cr
A \link{TreeAnalysis} object to add the search method to.}

\item{name}{[\code{character(1)}]\cr
The name of the search method. See \code{\link[=new_tree_search]{new_tree_search()}} for the full
list of available methods and accepted names.}

\item{...}{Optional named arguments passed to the constructor of the
selected command class. See the documentation for each class for
available parameters.}
}
\value{
A \strong{copy} of \code{tree_analysis} with the tree search command
added. The original object is not modified.
}
\description{
Create a tree search command object by name and add it to an existing
\link{TreeAnalysis}. This is a convenience wrapper around \code{\link[=new_tree_search]{new_tree_search()}}
that handles the creation and attachment in one step.
}
\examples{
\dontrun{
nex_path <- system.file("extdata", "canale_2022.nex", package = "nitro")
dm <- ReadAsPhyDat(nex_path) |> create_matrix()
ta <- make_tree_analysis(dm, outgroup = "Herrerasaurus")

# Traditional branch swapping search
ta <- set_tree_search(ta, "branch_swapping", replications = 100)

# Driven search with default sub-strategies
ta <- set_tree_search(ta, "driven_search")

# Inspect the search configuration
ta$commands$`tree search`$format()
}

}
\seealso{
\itemize{
\item \code{\link[=new_tree_search]{new_tree_search()}} — create a tree search command without attaching
it to an analysis.
\item \link{TreeAnalysis} — the analysis container class.
}
}
