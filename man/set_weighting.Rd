% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/new_weighting.R
\name{set_weighting}
\alias{set_weighting}
\title{Add a Weighting Method to a Tree Analysis}
\usage{
set_weighting(tree_analysis, name, ...)
}
\arguments{
\item{tree_analysis}{[\code{TreeAnalysis}]\cr
A \link{TreeAnalysis} object to add the weighting method to.}

\item{name}{[\code{character(1)}]\cr
The name of the weighting method. See \code{\link[=new_weighting]{new_weighting()}} for the full
list of available methods and accepted names.}

\item{...}{Optional named arguments passed to the constructor of the
selected command class. See \link{ImpliedWeightingCommand} and
\link{ExtendedImpliedWeightingCommand} for available parameters.}
}
\value{
A \strong{copy} of \code{tree_analysis} with the weighting command added.
The original object is not modified.
}
\description{
Create a weighting command object by name and add it to an existing
\link{TreeAnalysis}. This is a convenience wrapper around \code{\link[=new_weighting]{new_weighting()}}
that handles creation and attachment in one step.
}
\details{
\subsection{Automatic dependency handling}{

When \code{name} resolves to \code{"extended_implied_weighting"}, \code{set_weighting()}
creates an \link{ExtendedImpliedWeightingCommand}. The required
\link{ImpliedWeightingCommand} dependency is satisfied automatically when the
queue is executed: \link{ExtendedImpliedWeightingCommand}\verb{$enqueue()}
creates and enqueues a correctly configured \link{ImpliedWeightingCommand}
before adding itself. No manual handling of the dependency is required.

When \code{name} resolves to \code{"implied_weighting"}, only an
\link{ImpliedWeightingCommand} is created. Use \code{"extended_implied_weighting"}
if you need per-character concavity constants.
}
}
\examples{
\dontrun{
nex_path <- system.file("extdata", "canale_2022.nex", package = "nitro")
dm <- ReadPhyDat(nex_path) |> create_matrix()
ta <- make_tree_analysis(dm, outgroup = "Herrerasauridae")
ta <- set_tree_search(ta, "branch_swapping", replications = 100)

# Standard implied weighting
ta <- set_weighting(ta, "implied_weighting", concavity_constant = 6)

# Extended implied weighting with multi-constant mode
ta <- set_weighting(ta, "extended_implied_weighting",
                    concavity_constant = 6,
                    multi_constant     = TRUE,
                    proportion         = 0.3,
                    max_ratio          = 3)
}

}
\seealso{
\itemize{
\item \code{\link[=new_weighting]{new_weighting()}} — create a weighting command without attaching it
to an analysis.
\item \link{ImpliedWeightingCommand} — standard implied weighting.
\item \link{ExtendedImpliedWeightingCommand} — extended implied weighting with
per-character concavity constants.
\item \link{TreeAnalysis} — the analysis container class.
}
}
