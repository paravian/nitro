% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/CommandArgument-class.R
\name{CommandArgument}
\alias{CommandArgument}
\title{Command Argument}
\description{
An \link[R6:R6Class]{R6} class that stores a single named argument for a
\link{StandardCommand}, including its current value, default value, and
formatting rules for both TNT command output and human-readable display.

\code{CommandArgument} objects are created and managed internally by
\link{StandardCommand} via \verb{$new_argument()}. Users do not typically need to
instantiate this class directly.
}
\details{
\subsection{Write-once fields}{

\verb{$label}, \verb{$description}, \verb{$command_format}, \verb{$default_value}, and
\verb{$pretty_format} are all write-once — they can be set during
construction but cannot be changed afterwards. Only \verb{$value} is freely
mutable.
}

\subsection{Formatting}{

\verb{$render()} supports two modes and two value sources:\tabular{ll}{
   \code{mode} \tab Format used \cr
   \code{"command"} \tab \verb{$command_format} — produces the TNT command fragment. \cr
   \code{"pretty"} \tab \verb{$pretty_format} — produces a human-readable string. \cr
}
\tabular{ll}{
   \code{which} \tab Value used \cr
   \code{"value"} \tab The current \verb{$value}. \cr
   \code{"default_value"} \tab The \verb{$default_value}. \cr
}


Both \code{command_format} and \code{pretty_format} accept either a
\link[glue:glue]{glue} string (e.g., \code{"replic {value}"}) or a function
that takes a single \code{value} argument and returns a string. If
\code{pretty_format} is \code{NULL}, it defaults to \code{"{value}"}.
}
}
\seealso{
\itemize{
\item \code{\link[=execute_analysis]{execute_analysis()}} — the primary consumer of command queues.
\item \link{StandardCommand} — creates and manages \code{CommandArgument} objects via
\verb{$new_argument()}, \verb{$get_argument_value()}, and
\verb{$set_argument_value()}.
}
}
\keyword{internal}
\section{Active bindings}{
  \if{html}{\out{<div class="r6-active-bindings">}}
  \describe{
    \item{\code{command_format}}{[\code{character(1)} or \code{function}]\cr
\emph{(Write-once.)} A \link[glue:glue]{glue} string or function used to
format the argument value for inclusion in a TNT command string.}

    \item{\code{default_value}}{[\code{any}]\cr
\emph{(Write-once.)} The default value of the argument, used when no
user value has been supplied.}

    \item{\code{description}}{[\code{character(1)}]\cr
\emph{(Write-once.)} A human-readable description of the argument.}

    \item{\code{label}}{[\code{character(1)}]\cr
\emph{(Write-once.)} A unique string identifying this argument within
its parent \link{StandardCommand}.}

    \item{\code{pretty_format}}{[\code{character(1)}, \code{function}, or \code{NULL}]\cr
\emph{(Write-once.)} A \link[glue:glue]{glue} string or function used to
format the argument value for human-readable display (e.g., in
\verb{$format()} output). Defaults to \code{"{value}"} when \code{NULL}.}

    \item{\code{value}}{[\code{any}]\cr
The current value of the argument. Freely mutable; validation is
performed by the active binding in the parent \link{StandardCommand}
subclass.}
  }
  \if{html}{\out{</div>}}
}
\section{Methods}{
\subsection{Public methods}{
  \itemize{
    \item \href{#method-CommandArgument-initialize}{\code{CommandArgument$new()}}
    \item \href{#method-CommandArgument-render}{\code{CommandArgument$render()}}
    \item \href{#method-CommandArgument-clone}{\code{CommandArgument$clone()}}
  }
}
\if{html}{\out{<hr>}}
\if{html}{\out{<a id="method-CommandArgument-initialize"></a>}}
\if{latex}{\out{\hypertarget{method-CommandArgument-initialize}{}}}
\subsection{\code{CommandArgument$new()}}{
  Create a new \code{CommandArgument} object.

All fields except \verb{$value} become read-only after construction.
\code{CommandArgument} objects are normally created via
\link{StandardCommand}\verb{$new_argument()} rather than directly.
  \subsection{Usage}{
    \if{html}{\out{<div class="r">}}
    \preformatted{CommandArgument$new(
  label,
  description,
  command_format,
  default_value = NULL,
  pretty_format = NULL
)}
    \if{html}{\out{</div>}}
  }
  \subsection{Arguments}{
    \if{html}{\out{<div class="arguments">}}
    \describe{
      \item{\code{label}}{[\code{character(1)}]\cr
A unique string identifying the argument. See the \verb{$label} field.}
      \item{\code{description}}{[\code{character(1)}]\cr
A human-readable description. See the \verb{$description} field.}
      \item{\code{command_format}}{[\code{character(1)} or \code{function}]\cr
Format for TNT command output. See the \verb{$command_format} field.}
      \item{\code{default_value}}{[\code{any}]\cr
The default value (default: \code{NULL}). See the \verb{$default_value}
field.}
      \item{\code{pretty_format}}{[\code{character(1)}, \code{function}, or \code{NULL}]\cr
Format for human-readable display (default: \code{NULL}, uses
\code{"{value}"}). See the \verb{$pretty_format} field.}
    }
    \if{html}{\out{</div>}}
  }
  \subsection{Returns}{
    A new \code{CommandArgument} object.
  }
}

\if{html}{\out{<hr>}}
\if{html}{\out{<a id="method-CommandArgument-render"></a>}}
\if{latex}{\out{\hypertarget{method-CommandArgument-render}{}}}
\subsection{\code{CommandArgument$render()}}{
  Render the argument value as a formatted string.
  \subsection{Usage}{
    \if{html}{\out{<div class="r">}}
    \preformatted{CommandArgument$render(mode = "command", which = "value")}
    \if{html}{\out{</div>}}
  }
  \subsection{Arguments}{
    \if{html}{\out{<div class="arguments">}}
    \describe{
      \item{\code{mode}}{[\code{character(1)}]\cr
The format to use: \code{"command"} (default) for TNT command output,
or \code{"pretty"} for human-readable display.}
      \item{\code{which}}{[\code{character(1)}]\cr
Which value to format: \code{"value"} (default) for the current value,
or \code{"default_value"} for the default.}
    }
    \if{html}{\out{</div>}}
  }
  \subsection{Returns}{
    A single-element character vector containing the formatted
argument string.
  }
}

\if{html}{\out{<hr>}}
\if{html}{\out{<a id="method-CommandArgument-clone"></a>}}
\if{latex}{\out{\hypertarget{method-CommandArgument-clone}{}}}
\subsection{\code{CommandArgument$clone()}}{
  The objects of this class are cloneable with this method.
  \subsection{Usage}{
    \if{html}{\out{<div class="r">}}
    \preformatted{CommandArgument$clone(deep = FALSE)}
    \if{html}{\out{</div>}}
  }
  \subsection{Arguments}{
    \if{html}{\out{<div class="arguments">}}
    \describe{
      \item{\code{deep}}{Whether to make a deep clone.}
    }
    \if{html}{\out{</div>}}
  }
}

}
