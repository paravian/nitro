% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/resolve_dependencies.R
\name{resolve_dependencies}
\alias{resolve_dependencies}
\title{Resolve Command Dependencies}
\usage{
resolve_dependencies(commands, suppress_errors = TRUE)
}
\arguments{
\item{commands}{[\code{CommandList}]\cr
A \code{CommandList} object of commands whose required dependencies are to
be resolved.}

\item{suppress_errors}{[\code{logical(1)}]\cr
If \code{TRUE} (default), unmet dependencies cause the command to be
deferred silently. If \code{FALSE}, an error is raised immediately when a
dependency cannot be satisfied. This argument is passed through to
\code{\link[=resolve_command]{resolve_command()}} only when a deadlock is detected.}
}
\value{
A \link{CommandQueue} containing all commands in dependency-safe
order, ready to be passed to \link{TntInterface}\verb{$execute()}.
}
\description{
Resolve the required dependencies of a \code{CommandList} and return a single
\link{CommandQueue} ready for execution.

Each command is resolved against the commands that have already been
successfully resolved in the current pass. When a command is resolved,
its \link{CommandQueue} is built immediately by calling \verb{$enqueue()} and
cached. Because resolving one command may set a dependency on a
previously resolved command — changing the state of that command and
therefore the output of its \verb{$enqueue()} method — the function detects
these side effects and rebuilds the affected cached queues before
continuing. Once all commands have been resolved, the cached queues are
concatenated in resolution order and returned as a single
\link{CommandQueue}.

Resolution of optional dependencies is not performed here. It is
deferred to the \verb{$initialize()} method of \link{TntInterface}, which has
access to the fully assembled queue.
}
\details{
\subsection{Resolution algorithm}{

The function iterates over the unresolved command list in a \code{while}
loop. In each iteration, every remaining command is tested:
\enumerate{
\item If \verb{$is_resolved} is already \code{TRUE} (the command has no required
dependencies), it is enqueued immediately and moved to the resolved
list.
\item Otherwise, \code{\link[=resolve_command]{resolve_command()}} is called with the current resolved
list. If it succeeds, the command is enqueued and moved to the
resolved list.
\item After each successful resolution, the function checks whether
resolving the new command induced side effects in any previously
resolved command by comparing the current \verb{$format()} output of each
resolved command against a snapshot taken before the resolution
attempt. If a difference is detected, the cached queue for that
command is rebuilt by calling \verb{$enqueue()} again.
\item The resolved command's queue is also checked via \verb{$is_resolved}. If
the queue itself contains unresolved commands — commands spawned
inside \verb{$enqueue()} that depend on other commands in the queue —
those commands are resolved against the union of the global resolved
list and the commands already resolved within the queue.
\item If a complete iteration resolves no new commands, a deadlock is
detected and \code{\link[=resolve_command]{resolve_command()}} is called with
\code{suppress_errors = FALSE} on the first remaining command to produce
an informative error.
}
}

\subsection{Side-effect detection}{

Side effects arise when a command object is shared by reference between
two command objects — for example, when \link{BranchSupportCommand} holds a
reference to the same \link{BranchBreakingCommand} object that was
previously resolved and enqueued. Setting the \code{"tree search"}
dependency on \link{BranchSupportCommand} calls the dependency's validation
callback, which may mutate the shared object (e.g. setting
\code{set_only = TRUE}). The snapshot-and-diff mechanism detects this and
rebuilds the affected queue so that the rendered TNT script reflects
the updated state.
}
}
\seealso{
\itemize{
\item \code{\link[=resolve_command]{resolve_command()}} — resolves the dependencies of a single command
against a resolved list.
\item \code{\link[=execute_analysis]{execute_analysis()}} — the primary caller of this function.
\item \link{CommandQueue} — the queue class returned by this function.
\item \link{TntInterface} — resolves optional dependencies after the queue is
assembled.
}
}
\keyword{internal}
