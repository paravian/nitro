% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/new_tree_search.R
\name{new_tree_search}
\alias{new_tree_search}
\title{Create a Tree Search Configuration}
\usage{
new_tree_search(name, ...)
}
\arguments{
\item{name}{[\code{character(1)}]\cr
The name of the search method. Partial unambiguous matches are
accepted (case-insensitive, underscores optional). The available
methods are:\tabular{lll}{
   Name \tab Creates \tab TNT command \cr
   \code{"branch_breaking"} \tab \link{BranchBreakingCommand} \tab \code{bbreak} \cr
   \code{"branch_swapping"} \tab \link{BranchSwappingCommand} \tab \code{mult} \cr
   \code{"constrained_sectorial_search"} \tab \link{ConstrainedSectorialSearchCommand} \tab \code{sectsch} (css) \cr
   \code{"exclusive_sectorial_search"} \tab \link{ExclusiveSectorialSearchCommand} \tab \code{sectsch} (xss) \cr
   \code{"extra_methods_search"} \tab \link{ExtraSearchMethodsCommand} \tab \code{xmult} \cr
   \code{"random_sectorial_search"} \tab \code{RandomSectorialSearchCommand} \tab \code{sectsch} (rss) \cr
   \code{"ratchet"} \tab \link{RatchetCommand} \tab \code{ratchet} \cr
   \code{"tree_drifting"} \tab \link{TreeDriftingCommand} \tab \code{drift} \cr
   \code{"tree_fusing"} \tab \link{TreeFusingCommand} \tab \code{tfuse} \cr
   \code{"tree_hybridizing"} \tab \link{TreeHybridizingCommand} \tab \code{tfuse} (hybrid) \cr
}}

\item{...}{Optional named arguments passed to the constructor of the
selected command class. See the documentation for each class for
available parameters.}
}
\value{
A \link{TreeSearchCommand} object (the specific subclass depends on
\code{name}).
}
\description{
Create a new tree search command object by name. This is the recommended
way to instantiate tree search commands in \pkg{nitro}, rather than
calling R6 constructors directly.
}
\examples{
# Create a branch swapping search with defaults
bs <- new_tree_search("branch_swapping")

# Partial matching works
bs <- new_tree_search("branch_swap")

# Pass arguments to the constructor
bs <- new_tree_search("branch_swapping", replications = 100,
                       hold_rep = 25)

# Create an extra methods (driven) search
xm <- new_tree_search("extra_methods")

}
\seealso{
\itemize{
\item \code{\link[=set_tree_search]{set_tree_search()}} — create and add a tree search to a
\link{TreeAnalysis} in one step.
\item \link{BranchSwappingCommand}, \link{BranchBreakingCommand},
\link{ExtraSearchMethodsCommand}, \link{RatchetCommand},
\link{TreeDriftingCommand}, \link{TreeFusingCommand},
\link{TreeHybridizingCommand},
\link{ConstrainedSectorialSearchCommand},
\link{ExclusiveSectorialSearchCommand} — individual command classes
with full parameter documentation.
}
}
