% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/new_constraint.R
\name{set_constraint}
\alias{set_constraint}
\title{Add Topological Constraints to a Tree Analysis}
\usage{
set_constraint(tree_analysis, ...)
}
\arguments{
\item{tree_analysis}{[\code{TreeAnalysis}]\cr
A \link{TreeAnalysis} object to add the constraints to.}

\item{...}{One or more \link{MonophylyConstraint} or \link{BackboneConstraint}
objects.}
}
\value{
A \strong{copy} of \code{tree_analysis} with the constraint commands
added. The original object is not modified.
}
\description{
Create a \link{TopologicalConstraintsCommand} from one or more constraint
objects and add it to an existing \link{TreeAnalysis}. This is a convenience
wrapper that handles creation, queue integration, and attachment in one
step.
}
\details{
\subsection{Queue integration}{

\code{set_constraint()} adds a \link{TopologicalConstraintsCommand} at priority
\code{401} and a \link{ConstrainCommand} at priority \code{411}, ensuring that
constraints are defined and enforcement is activated before the tree
search begins.
}

\subsection{Backbone constraints and reference trees}{

When any supplied constraint is a \link{BackboneConstraint}, the reference
tree must be passed to \code{execute_analysis()} via the \code{reference_tree}
argument. The \code{"reference tree"} dependency on
\link{TopologicalConstraintsCommand} will be resolved automatically at that
point.
}
}
\examples{
\dontrun{
nex_path <- system.file("extdata", "canale_2022.nex", package = "nitro")
dm <- ReadAsPhyDat(nex_path) |> create_matrix()
ta <- make_tree_analysis(dm, outgroup = "Abelisauridae")
ta <- set_tree_search(ta, name = "branch_swapping", replications = 1000)

# Monophyly constraint
mc <- new_constraint("monophyly",
                     fixed_otus = c("TaxonA", "TaxonB", "TaxonC"))
ta <- set_constraint(ta, mc)

# Multiple constraints
mc2 <- new_constraint("monophyly",
                      fixed_otus  = c("TaxonD", "TaxonE"),
                      is_positive = FALSE)
ta <- set_constraint(ta, mc, mc2)

results <- execute_analysis(interface, ta, hold = 10000)
}

}
\seealso{
\itemize{
\item \code{\link[=new_constraint]{new_constraint()}} — create a constraint object without attaching it
to an analysis.
\item \link{MonophylyConstraint} — monophyly constraint type.
\item \link{BackboneConstraint} — backbone constraint type.
\item \link{TopologicalConstraintsCommand} — the command object created
internally by this function.
\item \code{\link[=execute_analysis]{execute_analysis()}} — pass \code{target_tree} here when using a backbone
constraint.
}
}
