% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/TntInterface-class.R
\name{TntInterface}
\alias{TntInterface}
\title{TNT Interface}
\description{
An \link[R6:R6Class]{R6} class that manages a TNT subprocess and provides the
low-level interface for sending commands and receiving output.

\code{TntInterface} is created via \code{\link[=create_interface]{create_interface()}} and passed to
\code{\link[=execute_analysis]{execute_analysis()}} to run a configured \link{TreeAnalysis}. Users do not
interact with the subprocess directly; command execution is handled
internally by \verb{$execute()}.
}
\details{
\subsection{Initialisation}{

When a \code{TntInterface} object is created, it:
\enumerate{
\item Locates and validates the TNT executable at \verb{$path}.
\item Detects the operating system platform (\code{"unix"} or \code{"windows"}).
\item Starts a TNT subprocess and handles the interactive licence agreement
prompt (Unix only — on Windows, the licence must be accepted manually
before calling \code{\link[=create_interface]{create_interface()}}).
\item Extracts and stores the TNT version string.
\item Kills the subprocess (a fresh one is started for each analysis via
\verb{$execute()}).
}
}

\subsection{Execution model}{

\verb{$execute()} writes all queued commands to a temporary \code{.tnt} script file and
starts a new TNT subprocess with that file as its argument. Output is read
incrementally until the TNT prompt is detected, then parsed and dispatched to
each command's \verb{$transform()} method. The result is returned as a
\link{TreeAnalysisResults} object.
}

\subsection{Platform differences}{

On Unix, TNT is run in a pseudo-terminal (PTY) to handle interactive
prompts. On Windows, stdout and stderr are captured via pipes. The
\verb{$platform} field is set automatically from \code{.Platform$OS.type}.
}

\subsection{Write-once fields}{

\verb{$path}, \verb{$platform}, and \verb{$version} are write-once: they are set during
initialisation and cannot be changed afterwards. \verb{$process} is mutable and
holds the current \link[processx:process]{processx::process} object.
}
}
\seealso{
\itemize{
\item \code{\link[=create_interface]{create_interface()}} — recommended way to create a \code{TntInterface}.
\item \code{\link[=execute_analysis]{execute_analysis()}} — runs a \link{TreeAnalysis} using this interface.
\item \link{TreeAnalysis} — the analysis configuration object.
\item \link{TreeAnalysisResults} — the object returned by \verb{$execute()}.
}
}
\keyword{internal}
\section{Active bindings}{
  \if{html}{\out{<div class="r6-active-bindings">}}
  \describe{
    \item{\code{path}}{[\code{character(1)}]\cr
\emph{(Write-once.)} The absolute path to the TNT executable.}

    \item{\code{platform}}{[\code{character(1)}]\cr
\emph{(Write-once.)} The operating system platform. One of \code{"unix"} or
\code{"windows"}. Set automatically from \code{.Platform$OS.type}.}

    \item{\code{process}}{[\code{process}]\cr
The current \link[processx:process]{processx::process} object representing the TNT
subprocess. Replaced each time \verb{$execute()} is called.}

    \item{\code{version}}{[\code{character(1)}]\cr
\emph{(Write-once.)} The version string of the TNT executable, in the
form \code{"{number} ({month} {year})"}. Set automatically during
initialisation.}
  }
  \if{html}{\out{</div>}}
}
\section{Methods}{
\subsection{Public methods}{
  \itemize{
    \item \href{#method-TntInterface-initialize}{\code{TntInterface$new()}}
    \item \href{#method-TntInterface-execute}{\code{TntInterface$execute()}}
    \item \href{#method-TntInterface-format}{\code{TntInterface$format()}}
    \item \href{#method-TntInterface-print}{\code{TntInterface$print()}}
    \item \href{#method-TntInterface-send_command}{\code{TntInterface$send_command()}}
    \item \href{#method-TntInterface-clone}{\code{TntInterface$clone()}}
  }
}
\if{html}{\out{<hr>}}
\if{html}{\out{<a id="method-TntInterface-initialize"></a>}}
\if{latex}{\out{\hypertarget{method-TntInterface-initialize}{}}}
\subsection{\code{TntInterface$new()}}{
  Strip and trim raw TNT output into a clean character vector.


  Start a TNT subprocess.


  Create a new \code{TntInterface} object.

Locates the TNT executable, handles the licence agreement prompt,
extracts the version string, and prepares the interface for use.
On Windows, the licence must be accepted manually before calling
this constructor. Use \code{\link[=create_interface]{create_interface()}} rather than calling this
directly.
  \subsection{Usage}{
    \if{html}{\out{<div class="r">}}
    \preformatted{TntInterface$new(path)}
    \if{html}{\out{</div>}}
  }
  \subsection{Arguments}{
    \if{html}{\out{<div class="arguments">}}
    \describe{
      \item{\code{path}}{[\code{character(1)}]\cr
Path to the TNT executable.}
      \item{\code{path}}{[\code{character(1)}]\cr
Path to the TNT executable.}
      \item{\code{value}}{[\code{character}]\cr
Raw output from the TNT subprocess.}
      \item{\code{argument}}{[\code{character(1)} or \code{NULL}]\cr
Optional argument to pass to the executable (e.g., a script path).}
    }
    \if{html}{\out{</div>}}
  }
  \subsection{Returns}{
    A character vector of trimmed, non-empty lines.
  }
}

\if{html}{\out{<hr>}}
\if{html}{\out{<a id="method-TntInterface-execute"></a>}}
\if{latex}{\out{\hypertarget{method-TntInterface-execute}{}}}
\subsection{\code{TntInterface$execute()}}{
  Execute a \link{CommandQueue} and return the results.

Writes all commands to a temporary TNT script file, starts a fresh
TNT subprocess, reads output incrementally until the TNT prompt is
detected, dispatches output to each command's \verb{$transform()} method,
and returns a \link{TreeAnalysisResults} object.
  \subsection{Usage}{
    \if{html}{\out{<div class="r">}}
    \preformatted{TntInterface$execute(queue)}
    \if{html}{\out{</div>}}
  }
  \subsection{Arguments}{
    \if{html}{\out{<div class="arguments">}}
    \describe{
      \item{\code{queue}}{[\code{CommandQueue}]\cr
A \link{CommandQueue} object produced by \code{\link[=execute_analysis]{execute_analysis()}}.}
    }
    \if{html}{\out{</div>}}
  }
  \subsection{Returns}{
    A \link{TreeAnalysisResults} object.
  }
}

\if{html}{\out{<hr>}}
\if{html}{\out{<a id="method-TntInterface-format"></a>}}
\if{latex}{\out{\hypertarget{method-TntInterface-format}{}}}
\subsection{\code{TntInterface$format()}}{
  Format the interface as a summary table.
  \subsection{Usage}{
    \if{html}{\out{<div class="r">}}
    \preformatted{TntInterface$format(...)}
    \if{html}{\out{</div>}}
  }
  \subsection{Arguments}{
    \if{html}{\out{<div class="arguments">}}
    \describe{
      \item{\code{...}}{Not used.}
    }
    \if{html}{\out{</div>}}
  }
  \subsection{Returns}{
    A \code{data.frame} showing the path, platform, and version.
  }
}

\if{html}{\out{<hr>}}
\if{html}{\out{<a id="method-TntInterface-print"></a>}}
\if{latex}{\out{\hypertarget{method-TntInterface-print}{}}}
\subsection{\code{TntInterface$print()}}{
  Print a brief summary of the interface.
  \subsection{Usage}{
    \if{html}{\out{<div class="r">}}
    \preformatted{TntInterface$print(...)}
    \if{html}{\out{</div>}}
  }
  \subsection{Arguments}{
    \if{html}{\out{<div class="arguments">}}
    \describe{
      \item{\code{...}}{Not used.}
    }
    \if{html}{\out{</div>}}
  }
}

\if{html}{\out{<hr>}}
\if{html}{\out{<a id="method-TntInterface-send_command"></a>}}
\if{latex}{\out{\hypertarget{method-TntInterface-send_command}{}}}
\subsection{\code{TntInterface$send_command()}}{
  Send a single command to an active TNT subprocess and return its
output.

This is a lower-level method used for interactive subprocess
communication. In normal use, commands are sent in batch via
\verb{$execute()}.
  \subsection{Usage}{
    \if{html}{\out{<div class="r">}}
    \preformatted{TntInterface$send_command(command)}
    \if{html}{\out{</div>}}
  }
  \subsection{Arguments}{
    \if{html}{\out{<div class="arguments">}}
    \describe{
      \item{\code{command}}{[\code{BasicCommand}]\cr
An object inheriting from \link{BasicCommand}.}
    }
    \if{html}{\out{</div>}}
  }
  \subsection{Returns}{
    A character vector of output lines from TNT.
  }
}

\if{html}{\out{<hr>}}
\if{html}{\out{<a id="method-TntInterface-clone"></a>}}
\if{latex}{\out{\hypertarget{method-TntInterface-clone}{}}}
\subsection{\code{TntInterface$clone()}}{
  The objects of this class are cloneable with this method.
  \subsection{Usage}{
    \if{html}{\out{<div class="r">}}
    \preformatted{TntInterface$clone(deep = FALSE)}
    \if{html}{\out{</div>}}
  }
  \subsection{Arguments}{
    \if{html}{\out{<div class="arguments">}}
    \describe{
      \item{\code{deep}}{Whether to make a deep clone.}
    }
    \if{html}{\out{</div>}}
  }
}

}
