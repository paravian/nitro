% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/make_tree_analysis.R
\name{make_tree_analysis}
\alias{make_tree_analysis}
\title{Create a Tree Analysis Configuration}
\usage{
make_tree_analysis(
  data,
  inactive_taxa = NULL,
  outgroup = NULL,
  zlb_rule = "minimum"
)
}
\arguments{
\item{data}{[\code{AbstractCharacterMatrix}, \code{MultiCharacterMatrix}]\cr
One or more \link{DiscreteMatrix} or \link{ContinuousMatrix} objects created by
\code{\link[=create_matrix]{create_matrix()}}.}

\item{inactive_taxa}{[\code{character} or \code{NULL}]\cr
Names of taxa to exclude from the analysis (default: \code{NULL}).}

\item{outgroup}{[\code{character(1)} or \code{NULL}]\cr
The name of the outgroup taxon (default: \code{NULL}).}

\item{zlb_rule}{[\code{character(1)}]\cr
The rule for collapsing zero-length branches (default: \code{"minimum"}).
See \link{CollapseRuleCommand} for the full list of options.}
}
\value{
A \link{TreeAnalysis} object with data and configuration commands
attached.
}
\description{
Create a new \link{TreeAnalysis} object with data, taxon activity, outgroup,
and zero-length branch rule configured in one step.

This is the recommended way to create a \link{TreeAnalysis} for use with
\code{\link[=execute_analysis]{execute_analysis()}}. After calling \code{make_tree_analysis()}, use
\code{\link[=set_tree_search]{set_tree_search()}}, \code{\link[=set_weighting]{set_weighting()}}, and \code{\link[=set_support]{set_support()}} to add
analysis commands.
}
\examples{
\dontrun{
# --- Discrete matrix only ---
nex_path <- system.file("extdata", "canale_2022.nex", package = "nitro")
dm <- ReadAsPhyDat(nex_path) |> create_matrix()

ta <- make_tree_analysis(
  data      = dm,
  outgroup  = "Abelisauridae",
  zlb_rule  = "minimum"
)

# --- Combined discrete + continuous matrix ---
nex_path2 <- system.file("extdata", "raven_2017.nex", package = "nitro")
csv_path  <- system.file("extdata", "raven_2017.csv", package = "nitro")
dm2 <- ReadAsPhyDat(nex_path2) |> create_matrix()
cm  <- read.table(csv_path, sep = ",", header = TRUE) |> create_matrix()

ta2 <- make_tree_analysis(
  data     = c(dm2, cm),
  zlb_rule = "minimum"
)
}

}
\seealso{
\itemize{
\item \code{\link[=create_matrix]{create_matrix()}} — creates the matrix objects required by \code{data}.
\item \code{\link[=set_tree_search]{set_tree_search()}} — add a tree search strategy.
\item \code{\link[=set_weighting]{set_weighting()}} — add an implied weighting configuration.
\item \code{\link[=set_support]{set_support()}} — add a support analysis.
\item \code{\link[=execute_analysis]{execute_analysis()}} — run the configured analysis.
\item \link{CollapseRuleCommand} — documents the available \code{zlb_rule} options.
}
}
