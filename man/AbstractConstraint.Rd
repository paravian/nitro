% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/AbstractConstraint-class.R
\name{AbstractConstraint}
\alias{AbstractConstraint}
\title{Abstract Constraint Base Class}
\description{
An \link[R6:R6Class]{R6} class that serves as the common base for all
topological constraint types in \pkg{nitro}.

\code{AbstractConstraint} defines the \verb{$is_positive} field shared by all
constraint types. A positive constraint requires that the specified
group appears in the recovered trees; a negative constraint requires
that it does not. It is not intended to be instantiated directly.
}
\details{
\subsection{Subclassing}{

Subclasses should call \code{super$initialize(is_positive)} in their own
\code{initialize()} method and define any additional fields and validation
logic required by the specific constraint type.

The two concrete subclasses are:
\itemize{
\item \link{MonophylyConstraint} — constrains a named set of taxa to be
monophyletic (or non-monophyletic).
\item \link{BackboneConstraint} — constrains the search to recover a topology
compatible with a reference tree.
}
}
}
\seealso{
\itemize{
\item \link{MonophylyConstraint} — monophyly constraint.
\item \link{BackboneConstraint} — backbone constraint.
\item \link{TopologicalConstraintsCommand} — the command object that holds one
or more constraints and renders the TNT \code{force} command.
\item \code{\link[=set_constraint]{set_constraint()}} — recommended way to attach constraints to a
\link{TreeAnalysis}.
}
}
\keyword{internal}
\section{Active bindings}{
  \if{html}{\out{<div class="r6-active-bindings">}}
  \describe{
    \item{\code{is_positive}}{[\code{logical(1)}]\cr
Whether the constraint is positive (\code{TRUE}, default) or negative
(\code{FALSE}). A positive constraint requires the specified group to
appear in recovered trees; a negative constraint requires that it
does not.}
  }
  \if{html}{\out{</div>}}
}
\section{Methods}{
\subsection{Public methods}{
  \itemize{
    \item \href{#method-AbstractConstraint-initialize}{\code{AbstractConstraint$new()}}
    \item \href{#method-AbstractConstraint-clone}{\code{AbstractConstraint$clone()}}
  }
}
\if{html}{\out{<hr>}}
\if{html}{\out{<a id="method-AbstractConstraint-initialize"></a>}}
\if{latex}{\out{\hypertarget{method-AbstractConstraint-initialize}{}}}
\subsection{\code{AbstractConstraint$new()}}{
  Create a new \code{AbstractConstraint} object.
  \subsection{Usage}{
    \if{html}{\out{<div class="r">}}
    \preformatted{AbstractConstraint$new(is_positive = TRUE)}
    \if{html}{\out{</div>}}
  }
  \subsection{Arguments}{
    \if{html}{\out{<div class="arguments">}}
    \describe{
      \item{\code{is_positive}}{[\code{logical(1)}]\cr
Whether the constraint is positive or negative (default: \code{TRUE}).
See the \verb{$is_positive} field.}
    }
    \if{html}{\out{</div>}}
  }
  \subsection{Returns}{
    A new \code{AbstractConstraint} object.
  }
}

\if{html}{\out{<hr>}}
\if{html}{\out{<a id="method-AbstractConstraint-clone"></a>}}
\if{latex}{\out{\hypertarget{method-AbstractConstraint-clone}{}}}
\subsection{\code{AbstractConstraint$clone()}}{
  The objects of this class are cloneable with this method.
  \subsection{Usage}{
    \if{html}{\out{<div class="r">}}
    \preformatted{AbstractConstraint$clone(deep = FALSE)}
    \if{html}{\out{</div>}}
  }
  \subsection{Arguments}{
    \if{html}{\out{<div class="arguments">}}
    \describe{
      \item{\code{deep}}{Whether to make a deep clone.}
    }
    \if{html}{\out{</div>}}
  }
}

}
